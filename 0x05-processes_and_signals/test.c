#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <unistd.h>

/**
 * main - Creates five zombie processes.
 * 
 * Return: Always 0.
 */
int main(void)
{
    char count = 0;
    printf("%c", count);
    printf("%d", count);
    return 0;
}